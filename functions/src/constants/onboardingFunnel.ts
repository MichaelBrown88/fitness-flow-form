/** Inclusive bounds for client-logged onboarding funnel steps (no PII). Matches useOnboarding logOnboardingStep calls (1–5). */
export const ONBOARDING_FUNNEL_STEP_MIN = 1;
export const ONBOARDING_FUNNEL_STEP_MAX = 5;
