rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Completely open for testing (We will tighten this later)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
